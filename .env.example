# Google Places API
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# OpenAI
OPENAI_API_KEY=your_openai_api_key_here

# Gmail (configurado via OAuth2 direto no n8n)
# Não requer variável — autenticar pelo painel do n8n

# n8n (se self-hosted)
N8N_HOST=localhost
N8N_PORT=5678
